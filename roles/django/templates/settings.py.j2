# This file was generated by ansible
# Do NOT modify this file!

{% for _import in django_settings_imports %}
{{_import}}
{% endfor %}

DATABASES = {
    'default': {
        'ENGINE': '{{django_settings_databases[0].default.ENGINE}}',
        'NAME': '{{django_settings_databases[0].default.NAME}}',
        'USER': '{{django_settings_databases[0].default.USER}}',
        'PASSWORD': '{{django_settings_databases[0].default.PASSWORD}}',
        'HOST': '{{django_settings_databases[0].default.HOST}}',
        'PORT': '{{django_settings_databases[0].default.PORT}}',
    }
}

# environment settings
{% for option in django_settings_additional %}
{{ option }}
{% endfor %}

import logging
logging.info('Local settings are loaded.')
